---
# symbols:
#   bin_dir: the dir dgen lives in
#   template_dir: the dir this template lives in
#   html_dir: changes for each document = document.name + '-html'

pandoc_options: 
    - '--template=%{template_dir}/template-body.html'

...

